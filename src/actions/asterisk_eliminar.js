import { createAction } from 'redux-act'

export const login = createAction('asterisk login')
export const logout = createAction('asterisk logout')
export const errorRequest = createAction('asterisk error request')
//export const putSip = createAction('asterisk put sip')
export const started = createAction('asteriek started')

export const initCall = createAction('asterisk init call')

//export const request = createAction('asterisk request')
